import { CustomerLoginResponse } from './customerlogin-response';

describe('LoginResponse', () => {
  it('should create an instance', () => {
    expect(new CustomerLoginResponse()).toBeTruthy();
  });
});
